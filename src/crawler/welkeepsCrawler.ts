import { Browser } from 'puppeteer';
import TelegramChatBot from '../lib/TelegramChatBot';

const welkeepsCrawler = (browser: Browser, chatbot: TelegramChatBot) => {
  console.log('start welkeeps');
};

export default welkeepsCrawler;